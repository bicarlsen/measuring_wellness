<h1>Analyte Groups</h1>
<%= link_to 'Admin', admin_path %>
<%= render 'shared/flash' %>

<ul id='analyte-groups'>
	<% @groups.each do |group| %>
		<li><%= link_to group.name, group %></li>
	<% end %>
</ul>

<section>
	<%= link_to "Add New Group", new_analyte_group_path %>
</section>

