<% 
	locals = { title: title, content: content,
		bucket_type: "measure-summary", link_base: "measures" } 
	locals[:image] = image if defined? image
%>
<%= render partial: 'bucket', locals: locals %>
